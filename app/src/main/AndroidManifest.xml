<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.musthafa.carparking">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA"> </uses-permission>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_parkme"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Activity.Activity.MainActivity"
            android:theme="@style/NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.Activity.AdminHome" />
        <activity android:name=".Activity.Activity.AddMall" />
        <activity android:name=".Activity.Activity.ShobhaMall" />
        <activity android:name=".Activity.Activity.AddLocation" />
        <activity android:name=".Activity.Activity.SlotOne" />
        <activity android:name=".Activity.Activity.Payment" />
        <activity android:name=".Activity.Activity.UserHome" />
        <activity android:name=".Activity.Activity.Registration" />
        <activity android:name=".Activity.Activity.ViewMall" />
        <activity android:name=".Activity.Activity.PaymentSuccess" />
        <activity android:name=".Activity.Activity.MallHome" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Activity.Activity.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".Activity.Activity.ViewMySlot" />
        <activity android:name=".Activity.Activity.ViewPayment" />
        <activity android:name=".Activity.Activity.AddArea" />
        <activity android:name=".Activity.Activity.ExampleAreaAdd" />
        <activity android:name=".Activity.Activity.PaymentError" />
        <activity android:name=".Activity.Activity.LocationFinder"></activity>
    </application>

</manifest>